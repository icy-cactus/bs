<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.MerchantDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.entity.MerchantEntity" id="merchantMap">
        <result property="merchantusername" column="merchantusername"/>
        <result property="password" column="password"/>
        <result property="merchantname" column="merchantname"/>
        <result property="merchantaddress" column="merchantaddress"/>
        <result property="email" column="email"/>
        <result property="phonenumber" column="phonenumber"/>
        <result property="merchantlicense" column="merchantlicense"/>
    </resultMap>



    <select id="selectListView"
		resultType="com.entity.view.MerchantView" >

		SELECT  merchant.* FROM merchant  merchant
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
	<select id="selectView"
		resultType="com.entity.view.MerchantView" >
		SELECT * FROM merchant  merchant <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	

</mapper>
